<!-- NAVBAR -->

<nav class="navbar navbar-expand-lg bg-body-tertiary fixed-top shadow-sm">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">WILDPACKER</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_article_index') }}">Articles récents</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_category_index') }}">Catégories</a>
				</li>
				<li class="nav-item dropdown">
					{% if app.user and 'ROLE_ADMIN' in app.user.roles %}
						<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							Espace Administrateur
						</a>
					{% endif %}

					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{ path('app_admin_category_index') }}">Catégories</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{ path('app_admin_article_index') }}">Articles</a>
						</li>
						<li><hr class="dropdown-divider"></li>
						<li>
							<a class="dropdown-item" href="{{ path('app_admin_user_index') }}">Utilisateurs</a>
						</li>
					</ul>
				</li>
			</ul>
			{# <form class="d-flex" role="search">
																						<input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
																						<button class="btn btn-outline-success" type="submit">Search</button>
																					</form> #}
			<a href="{{ path('app_article_new') }}" class="btn btn-primary">Create new post</a>

			{% if app.user %}
				<span class="nav-link mx-3 text-uppercase">{{ app.user.lastname }}
					{{ app.user.firstname }}</span>
				<a href="{{ path('app_logout') }}" class="btn btn-primary me-2">Logout</a>
			{% else %}
				<a class="nav-link mx-2" href="{{ path('app_login') }}">Login</a>
				<a class="nav-link mx-2" href="{{ path('app_register') }}">Register</a>
			{% endif %}
		</div>
	</div>
</nav>
